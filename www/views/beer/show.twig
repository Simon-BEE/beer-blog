{% extends "/layout/default.twig" %}
{% block title %}{{ title }}{% endblock %}
{% block content %}
<section class="post seeking">
    <article class="card">
        <h2>{{ beer.name }}</h2>
        <div class="post-content">
            <img src="{{ beer.img }}" alt="random pic" width="200" height="600">
            <p>{{ beer.content }}</p>
            <p>{{ (beer.price * 1.2) | number_format(2, ',', '.') }}€</p>
        </div>
    </article>
</section>
{% endblock %}
